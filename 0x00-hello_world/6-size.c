#!/bin/bash
#include stdio.h
/*
 * a program that prints size of a types
 */
int main(void)
{
char a;
int b;
long int c;
long long int d;
float;
printf("Size of a Char: %lu byte(s)\n, (unsigned long)sizeof(a));
return(0);
}
